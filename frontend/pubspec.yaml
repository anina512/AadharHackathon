name: firebase_upload_example
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  advance_pdf_viewer: ^2.0.0
  cupertino_icons: ^1.0.2

  # upload file to firebase
  firebase_core: ^1.0.3
  firebase_storage: ^8.0.3

  # pick file
  file_picker: ^3.0.1
  image_picker: ^0.8.4+4
  flutter_genius_scan: ^4.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true